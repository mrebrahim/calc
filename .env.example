# Supabase — both values are *publishable* client credentials.
# They are safe to ship in the app binary; every table is protected by RLS
# scoped to auth.uid(), so a key on its own reads nothing.
EXPO_PUBLIC_SUPABASE_URL=https://nhgxcxkoicablnltjets.supabase.co
EXPO_PUBLIC_SUPABASE_KEY=sb_publishable_2TZFWBEpwdBO23AtkhbI5A_b_-lqsjZ
